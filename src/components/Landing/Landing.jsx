import css from "./Landing.module.scss";

export const Landing = () => {
  return (
    <section className={`paddings ${css.wrapper}`}>
      <div className={`innerWidth ${css.container}`}>Landing</div>
    </section>
  );
};
